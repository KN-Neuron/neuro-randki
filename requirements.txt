flask
numpy
scipy
# PyTorch CPU (zainstaluj ręcznie jeśli skrypt tego nie zrobi):
# pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# BrainAccess SDK (instalacja z pakietu producenta):
# https://brainaccess.io/sdk
# Bez SDK apka działa w trybie mock (losowy sygnał EEG).
